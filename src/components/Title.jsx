import React from 'react';
import styled from 'styled-components';

const HeaderTitle = styled.h1`
  font-size: 4em;
  text-align: center;
`;

const Title = () => (
  <HeaderTitle>Tutor Finder</HeaderTitle>
);

export default Title;